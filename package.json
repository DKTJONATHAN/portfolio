{
  "name": "portfolio",
  "private": true,
  "dependencies": {
    "@octokit/rest": "^19.0.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
{
  "name": "jonathan-mwaniki-site",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i tailwind.css -o styles.css --minify",
    "dev": "tailwindcss -i tailwind.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}